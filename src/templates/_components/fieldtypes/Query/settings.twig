{% import "_includes/forms" as forms %}

{% namespace 'query' %}
    {% from _self import query %}
    {{ query(field.query, querySettings) }}
{% endnamespace %}

{% macro query(query, querySettings) %}
    {% import "_includes/forms" as forms %}
    {{ querySettings.inputHtml()|raw }}
{% endmacro %}